@page "/announcements/{Id}"
@if (_message == null)
{
    <MudProgressCircular Color="Color.Default" Indeterminate="true" />
}
else
{
    <MudText Typo="Typo.h3">@_message.Title</MudText>
    
    <DynamicComponent Type="@_message.ContentComponent" />
}

@code {
    
}