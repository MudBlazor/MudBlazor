@namespace MudBlazor.Docs.Examples

<div>
    <MudChart ChartType="ChartType.Pie" InputData="@data" InputLabels="@labels" DisplayLegendItemValues="true" />
    <MudButton @onclick="RandomizeData">Randomize Data</MudButton>
</div>

@code {
    public double[] data = { 77, 25, 20, 5 };
    public string[] labels = { "Uranium", "Plutonium", "Thorium", "Caesium" };

    Random random = new Random();
    public void RandomizeData()
    {
        var new_data = new double[data.Length];
        for (int i = 0; i < new_data.Length; i++)
            new_data[i] = random.NextDouble() * 100;
        data = new_data;
        StateHasChanged();
    }
}