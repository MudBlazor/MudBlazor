@page "/components/buttonfab"

<DocsPage>
    <DocsPageHeader Title="Floating Action Button" Component="@nameof(MudFab)" />
    <DocsPageContent>

        <DocsPageSection>
            <SectionHeader Title="Simple FAB">
                <Description>The simplest FAB with no variant set, using the default <CodeInline>Variant.Filled</CodeInline>.</Description>
            </SectionHeader>
            <SectionContent Code="@nameof(FabSimpleExample)">
                <FabSimpleExample />
            </SectionContent>
        </DocsPageSection>

        <DocsPageSection>
            <SectionHeader Title="Filled FABs">
                <Description>Filled FABs have elevation (box shadow) and is raised on click by default.</Description>
            </SectionHeader>
            <SectionContent Code="@nameof(FabFilledExample)">
                <FabFilledExample />
            </SectionContent>
        </DocsPageSection>

        <DocsPageSection>
            <SectionHeader Title="Text FABs">
                <Description>Text FABs have no drop shadow, background or border and only a hover effect is used. The <CodeInline>Color</CodeInline> property only applies to the icon and label.</Description>
            </SectionHeader>
            <SectionContent Code="@nameof(FabTextExample)">
                <FabTextExample />
            </SectionContent>
        </DocsPageSection>

        <DocsPageSection>
            <SectionHeader Title="Outlined FABs">
                <Description>Outlined FABs are similar to Text FABs except for the border that inherits its color from the set <CodeInline>Color</CodeInline> property.</Description>
            </SectionHeader>
            <SectionContent Code="@nameof(FabOutlinedExample)">
                <FabOutlinedExample />
            </SectionContent>
        </DocsPageSection>

        <DocsPageSection>
            <SectionHeader Title="Size">
                <Description></Description>
            </SectionHeader>
            <SectionContent Code="@nameof(FabSizeExample)" ShowCode="false">
                <FabSizeExample />
            </SectionContent>
        </DocsPageSection>

    </DocsPageContent>
</DocsPage>
