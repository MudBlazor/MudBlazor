@inherits LayoutComponentBase

<MudPopoverProvider/>
<MudDialogProvider/>
<MudSnackbarProvider/>

@Body

@code {
    static MainLayout()
    {
        MudGlobal.TooltipDefaults.Delay = TimeSpan.FromMilliseconds(500);
    }
}
