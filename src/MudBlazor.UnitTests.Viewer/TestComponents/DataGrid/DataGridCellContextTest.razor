<MudDataGrid T="Model" Items="@_items" CommittedItemChanges="@CommittedItemChanges">
    <Columns>
        <PropertyColumn Property="x => x.Name" />
        <PropertyColumn Property="x => x.Age" />
    </Columns>
</MudDataGrid>

@code {
    private readonly IEnumerable<Model> _items = new List<Model>
    {
        new("John", 45), 
        new("Johanna", 23), 
        new("Steve", 32)
    };

    [Parameter]
    public Func<Model, Task<DataGridEditFormAction>>? CommittedItemChanges { get; set; }

    public record Model(string Name, int Age);
}
