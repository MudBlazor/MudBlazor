<div>
    <label for="birthday">Birthday (using InputId with label binding)</label>
    <MudDatePicker InputId="birthday" @bind-Date="Birthday" />
</div>

@code {
    public static string __description__ = "Date picker with InputId parameter";
    
    private DateTime? Birthday { get; set; }
}
