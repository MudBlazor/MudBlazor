@if (FormatFirst)
{
    <MudDateRangePicker TValue="DateTime?" DateFormat="yyyy MMMM dd" DateRange="@(DateRange)"/>
}
else
{
    <MudDateRangePicker TValue="DateTime?" DateRange="@(DateRange)" DateFormat="yyyy MMMM dd"/>
}

@code {
    [Parameter, EditorRequired]
    public required DateRange<DateTime?> DateRange { get; set; }

    [Parameter]
    public bool FormatFirst { get; set; }
}