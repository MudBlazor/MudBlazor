<MudForm ValidationDelay="0">
    <MudTextField T="string" Required="true" RequiredError="Enter a rock star"></MudTextField>
    <MudDatePicker @bind-Date="_date"></MudDatePicker>
</MudForm>

@code {
    public static string __description__ = "Changing any of the fields, required or not, should set the forms IsTouched property to true";
    
    private DateTime? _date = new DateTime(2021, 05, 15);
}
