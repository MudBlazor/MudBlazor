<MudDatePicker @bind-Date="_birthDate" Validation="ValidateValueAsync"/>
<MudDatePicker @bind-Date="_birthDate" Validation="ValidateValue"/>

@code {
    private DateTime? _birthDate = DateTime.Now;

    private static Func<object, string, Task<IEnumerable<string>>> ValidateValueAsync => (_, _) => 
        Task.FromResult<IEnumerable<string>>(Array.Empty<string>());
    
    private static Func<object, string, IEnumerable<string>> ValidateValue => (_, _) => 
        Array.Empty<string>();
}