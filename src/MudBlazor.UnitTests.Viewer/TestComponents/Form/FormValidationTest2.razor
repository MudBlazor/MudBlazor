@namespace MudBlazor.UnitTests.TestComponents

<MudForm ValidationDelay="0">
    <MudTextField T="string" Required="true" RequiredError="Enter a star" Validation="@validation"></MudTextField>
    <MudSelect T="string" Required="false" >
        <MudSelectItem T="string" Value="@("foo")" />
        <MudSelectItem T="string" Value="@("bar")" />
    </MudSelect>
</MudForm>

@code {
    public static string __description__ = "Form should become valid as soon as all required fields are filled in correctly.";

    [Parameter] public object validation { get; set; }
}
