@namespace MudBlazor.UnitTests.TestComponents

<MudChipSet T="string" @bind-SelectedChips="SelectedChipArray" MultiSelection="true" Filter="true">
    @if (_extraItem)
    {
        <MudChip T="string" Text="Extra Chip" Default="true"></MudChip>

    }
    <MudChip T="string" Text="Primary" Default="true"></MudChip>
</MudChipSet>
Add chips <MudIconButton Icon="@Icons.Material.Filled.Add" OnClick="Enable">Enable</MudIconButton>


@code
{ 
    bool _extraItem = false;
    public void Enable() => _extraItem = !_extraItem;
    MudChip<string>[] SelectedChipArray;
}