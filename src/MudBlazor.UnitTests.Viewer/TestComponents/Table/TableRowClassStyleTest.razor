@namespace MudBlazor.UnitTests.TestComponents

<MudTable T="int" Items="items" RowStyleFunc="@((item, _) => item > 1 ? "color: blue" : "color: red")" RowClassFunc="@((_, index) => index % 2 == 0 ? "even" : "odd")">
    <HeaderContent>
        <MudTh>#</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd>@context</MudTd>
    </RowTemplate>
</MudTable>

@code {
    public static string __description__ = "The row style and row class should be placed on the row correctly.";

    int[] items = {0,1,2,3};

}
