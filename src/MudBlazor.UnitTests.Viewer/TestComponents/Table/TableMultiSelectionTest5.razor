@namespace MudBlazor.UnitTests.TestComponents

<MudText>SelectedItems { @string.Join(", ", selectedItems) }</MudText>
<MudTable Items="items" @bind-SelectedItems="selectedItems" MultiSelection="true" RowsPerPage="2">
    <HeaderContent>
        <MudTh>#</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd>@context</MudTd>
    </RowTemplate>
    <PagerContent>
        <MudTablePager PageSizeOptions="pageSizeOptions"/>
    </PagerContent>
</MudTable>

@code {
    public static string __description__ = "Multiselection should work with paging.";
    int[] pageSizeOptions = new int[] {2};
    int[] items = new int[]{0,1,2,3};
    HashSet<int> selectedItems=new HashSet<int>() { 0, 1, 2, 3 };
}
