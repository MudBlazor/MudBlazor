@using MudBlazor.Utilities

<MudHotkey Key="SelectedKey" KeyModifiers="_selectedKeyModifiers" OnHotkeyPressed="OnHotKey" HideChildContentOnRepress="HideChildContentOnRepress" Disabled="Disabled">
    <div id="hotkey-child">
        Child Content
    </div>
</MudHotkey>

@code {
    private const JsKey SelectedKey = JsKey.KeyA;
    private readonly IEnumerable<JsKeyModifier> _selectedKeyModifiers = [JsKeyModifier.Control];

    public int PressedCount { get; private set; }

    [Parameter]
    public bool Disabled { get; set; }

    [Parameter]
    public bool HideChildContentOnRepress { get; set; }

    private void OnHotKey()
    {
        PressedCount++;
    }
}
