<MudPopoverProvider />

<MudSelect @bind-Value="_value" PopoverClass="select-popover-class" ShrinkToContent="_shinkToContent">
    <MudSelectItem Value="@("1")"/>
    <MudSelectItem Value="@("2")"/>
    <MudSelectItem Value="@("3")"/>
    <MudSelectItem Value="@("4")" Disabled="true"/>
</MudSelect>

<MudSwitch @bind-Value="_shinkToContent" Class="mt-4" Color="Color.Primary">Fit Content</MudSwitch>

@code {
    public static string __description__ = "Value 2 should be selected initially and highlighted.";

    private bool _shinkToContent;
    private string? _value = "2";
}
