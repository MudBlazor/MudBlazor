<MudPopoverProvider />

<MudSelect @bind-Value="_value" Clearable="true" OnClearButtonClick="@ClearButtonClickHandler" ShrinkToContent="ShrinkToContent">
    <MudSelectItem Value="@("1")" />
    <MudSelectItem Value="@("2")" />
    <MudSelectItem Value="@("3")" />
</MudSelect>

<MudSwitch @bind-Value="ShrinkToContent" Class="mt-4" Color="Color.Primary">Shrink To Content</MudSwitch>

@code {
    public static string __description__ = "Initially hidden clear button should show when item is selected.";

    private string? _value;

    [Parameter]
    public bool ShrinkToContent { get; set;}

    public bool ClearButtonClicked { get; private set; }

    private void ClearButtonClickHandler(MouseEventArgs e)
    {
        ClearButtonClicked = true;
    }
}
