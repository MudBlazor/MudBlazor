<MudPopoverProvider />

<MudSelect T="string" ValueChanged="ValueChanged" SelectedValuesChanged="ValuesChanged" MultiSelection="MultiSelection" FitContent="_fitContent">
    <MudSelectItem T="string" Value="@("1")" />
    <MudSelectItem T="string" Value="@("2")" />
    <MudSelectItem T="string" Value="@("3")" />
    <MudSelectItem T="string" Value="@("4")" Disabled="true" />
</MudSelect>

<MudSwitch @bind-Value="_fitContent" Class="mt-4" Color="Color.Primary">Fit Content</MudSwitch>

@code {
    private bool _fitContent;

    [Parameter]
    public bool MultiSelection { get; set; }

    public int ValueChangeCount { get; set; }

    public int ValuesChangeCount { get; set; }

    private void ValueChanged()
    {
        ValueChangeCount++;
    }

    private void ValuesChanged()
    {
        ValuesChangeCount++;
    }
}
