@if (MustBeShown)
{
    <AutocompleteContainerTest @bind-HasBeenDisposed="@HasBeenDisposed"></AutocompleteContainerTest>
}

@code {
    public bool MustBeShown { get; set; } = true;

    public bool HasBeenDisposed { get; private set; }
}
