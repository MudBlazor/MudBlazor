@namespace MudBlazor
@inherits MudBaseButton

<button @attributes="UserAttributes" type="button" class="@Classname" style="@Style" @onclick="OnClickHandler" href="@Link" disabled="@Disabled">
    <span class="mud-button-label">
        @if (!string.IsNullOrWhiteSpace(StartIcon))
        {
            <span class="@IconClassname">
                <MudIcon Icon="@StartIcon" Size="@Size"></MudIcon>
            </span>
        }
        @ChildContent
        @if (!string.IsNullOrWhiteSpace(@EndIcon))
        {
            <span class="@IconClassname">
                <MudIcon Icon="@EndIcon" Size="@Size"></MudIcon>
            </span>
        }
    </span>
</button>
