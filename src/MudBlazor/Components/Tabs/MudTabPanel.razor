@namespace MudBlazor
@inherits MudComponentBase
@implements IAsyncDisposable

@if (Parent?.KeepPanelsAlive == true)
{
    <div @attributes="UserAttributes" class="@PanelClassname" style="@Stylename" role="tabpanel" id="@Parent.GetTabPanelId(this)" aria-labelledby="@Parent.GetTabId(this)">
        @ChildContent
    </div>
}
else
{
    @if (Parent?.ActivePanel == this)
    {
        <div @attributes="UserAttributes" class="@PanelClassname" style="@Stylename" role="tabpanel" id="@Parent.GetTabPanelId(this)" aria-labelledby="@Parent.GetTabId(this)">
            @ChildContent
        </div>
    }
}
