name: build-test-mudblazor

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  build-test:
    name: Build and test mudblazor projects
    uses: xC0dex/Workflows/.github/workflows/template-build-test.yml@build/dotnet-9
    secrets: inherit
    with:
      check-eslint: 'MudBlazor/TScripts'
      publish-coverage: true
